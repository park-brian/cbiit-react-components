import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <pre>To browse components, run "npm run storybook" and navigate to localhost:9009</pre>
  </React.StrictMode>,
  document.getElementById('root')
);
